export function combineAll(lists){const entries=Object.entries(lists).filter(([,arr])=>Array.isArray(arr)&&arr.every(v=>typeof v==='number'));const filtered=Object.fromEntries(entries);const keys=Object.keys(filtered);if(keys.length===0)return [];const res=[];function bt(i,cur){if(i===keys.length){res.push([...cur]);return}const arr=filtered[keys[i]];for(const v of arr){cur.push(v);bt(i+1,cur);cur.pop()}}bt(0,[]);return res.map(t=>{const o={};for(let j=0;j<keys.length;j++)o[keys[j]]=t[j];return o})}
export default combineAll;