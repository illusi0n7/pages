export function combineAll(lists){const entries=Object.entries(lists);if(entries.length===0)return [];const res=[];function bt(i,cur){if(i===entries.length){res.push([...cur]);return}const [,arr]=entries[i];for(const v of arr){cur.push(v);bt(i+1,cur);cur.pop()}}bt(0,[]);const keys=Object.keys(lists);return res.map(t=>{const o={};for(let j=0;j<keys.length;j++)o[keys[j]]=t[j];return o})}
export default combineAll;